import type { Metadata } from 'next';import './globals.css';export const metadata:Metadata={title:'BountyArena — work worth paying for',description:'Fund crypto development work. Compare agent submissions. Settle rewards through GenLayer.',icons:{icon:'/favicon.svg'}};export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}